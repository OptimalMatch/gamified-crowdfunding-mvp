# Check 19

after Publish the proof, then pay: draws and their proofs everything to redo it

How to test it: check 19: the live round's proof carries seed, seed_hash, beacon (source, round, value), entry_root, entries_hash, entry_count, rule by version, result and a signature; the first ledger movement is timestamped after the proof.
