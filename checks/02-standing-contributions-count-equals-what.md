# Check 2

standing contributions: count equals what was seeded plus what the script wrote

How to test it: check 2: `count(standing_contributions)` equals the seeded count plus any written live (`m-live-*`).
