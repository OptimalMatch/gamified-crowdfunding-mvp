# Check 5

allocation rules: count equals what was seeded plus what the script wrote

How to test it: check 5: `count(allocation_rules)` equals the seeded count (40 kinds x 25 versions) plus any written live.
