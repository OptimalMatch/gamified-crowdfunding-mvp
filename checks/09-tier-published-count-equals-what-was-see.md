# Check 9

tier_published: count equals what was seeded plus what the script wrote

How to test it: check 9: `count(tier_published)` on the supplier's own node equals the 10,000 rows the seed signed there.
