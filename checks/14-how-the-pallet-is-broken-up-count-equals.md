# Check 14

how the pallet is broken up: count equals what was seeded plus what the script wrote

How to test it: check 14: `count(pallet_splits)` equals the seeded ones plus one per live buy addressed.
