# Check 1

backers, wallets and standing: count equals what was seeded plus what the script wrote

How to test it: checks/run.mjs, check 1: `count(backers)` equals the seed manifest's count plus the newcomers the join script signed up (ids `b-new-*`).
