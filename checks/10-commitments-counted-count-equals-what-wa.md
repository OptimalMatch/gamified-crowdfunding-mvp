# Check 10

commitments, counted: count equals what was seeded plus what the script wrote

How to test it: check 10: `count(commitments)` equals the seeded receipts plus every live join, and `count(commitments_counted)` the seeded counters plus one per live window.
