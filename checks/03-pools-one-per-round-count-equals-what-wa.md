# Check 3

pools, one per round: count equals what was seeded plus what the script wrote

How to test it: check 3: `count(pools)` equals the seeded count plus the rounds opened live (`-live-`, `-web-`, `-draft-`).
